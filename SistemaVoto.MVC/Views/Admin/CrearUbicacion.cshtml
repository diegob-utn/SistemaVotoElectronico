@using SistemaVoto.Modelos
@using SistemaVoto.MVC.ViewModels
@model UbicacionViewModel
@{
    ViewData["Title"] = "Crear Ubicación";
    var ubicaciones = ViewBag.Ubicaciones as List<Ubicacion> ?? new List<Ubicacion>();
}

<div class="auth-container" style="max-width: 600px; margin: 2rem auto;">
    <div class="auth-card">
        <div class="auth-header">
            <a href="@Url.Action("Ubicaciones")" class="auth-back-link">
                <i data-lucide="arrow-left" style="width: 16px; height: 16px;"></i> Volver
            </a>
            <h1 class="auth-title">
                <i data-lucide="map-pin" style="width: 24px; height: 24px;"></i>
                Nueva Ubicación
            </h1>
        </div>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-error">
                <i data-lucide="alert-circle" style="width: 20px; height: 20px;"></i>
                @Model.ErrorMessage
            </div>
        }

        <form asp-action="CrearUbicacion" method="post" class="auth-form">
            <div class="form-group">
                <label asp-for="Nombre" class="form-label"></label>
                <div class="input-group">
                    <span class="input-icon"><i data-lucide="type"></i></span>
                    <input asp-for="Nombre" class="form-input" placeholder="Ej. Provincia de San José" />
                </div>
                <span asp-validation-for="Nombre" class="text-error"></span>
            </div>

            <div class="form-group">
                <label asp-for="Tipo" class="form-label"></label>
                <div class="input-group">
                    <span class="input-icon"><i data-lucide="tag"></i></span>
                    <select asp-for="Tipo" class="form-input" style="appearance: auto;">
                        <option value="">-- Seleccione Tipo --</option>
                        <option value="PAIS">PAIS</option>
                        <option value="PROVINCIA">PROVINCIA</option>
                        <option value="CANTON">CANTON</option>
                        <option value="CIUDAD">CIUDAD</option>
                        <option value="PARROQUIA">PARROQUIA</option>
                        <option value="CAMPUS">CAMPUS</option>
                        <option value="OTRO">OTRO</option>
                    </select>
                </div>
                <span asp-validation-for="Tipo" class="text-error"></span>
            </div>

            <div class="form-group">
                <label asp-for="ParentId" class="form-label">Ubicación Superior (Opcional)</label>
                <div class="input-group">
                    <span class="input-icon"><i data-lucide="arrow-up-circle"></i></span>
                    <select asp-for="ParentId" class="form-input" style="appearance: auto;">
                        <option value="">-- Ninguna (Raíz) --</option>
                        @foreach (var u in ubicaciones)
                        {
                            <option value="@u.Id">@u.Nombre (@u.Tipo)</option>
                        }
                    </select>
                </div>
            </div>

            <button type="submit" class="btn btn-primary btn-block">
                <i data-lucide="save" style="width: 20px; height: 20px;"></i> Crear
            </button>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            lucide.createIcons();
        });
    </script>
}
